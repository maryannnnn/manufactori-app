build
dist / media
node_modules
.DS_Store
.env
.env.local
.next
next-env.d.ts
.vercel

# Payload writes uploads to public/media (Media collection staticDir).
# Production originals and Payload image sizes are Git-tracked so they
# deploy with Git → Vercel. Ignore only junk that may appear beside them.
public/media/.DS_Store
public/media/**/.DS_Store

public/robots.txt
public/sitemap*.xml


# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
